import { WorkflowApp } from "../workflows/WorkflowApp";

/** Axis: interaction model — translucent materials / glass chrome. */
export function CommandSurface() {
  return <WorkflowApp tone="glass" />;
}
