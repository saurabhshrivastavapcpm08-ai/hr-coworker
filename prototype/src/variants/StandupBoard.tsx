import { WorkflowApp } from "../workflows/WorkflowApp";

/** Axis: personality — narrative coworker standup; editorial emphasis. */
export function StandupBoard() {
  return <WorkflowApp tone="editorial" />;
}
